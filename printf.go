package main

import "fmt"

func main() {
	var j string = "hello"
	var k int = 15
	fmt.Printf("j has the value of: %V and type: %T\n", j, j)
	fmt.Printf("k has the value of : %V and type: %T\n", k, k)
}
